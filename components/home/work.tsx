"use client";

import Subtitle from "../subtitle";

const Work = () => {
	return (
		<div className="items-center justify-center w-full flex flex-col gap-4">
			<Subtitle text="WORK" />
		</div>
	);
};

export default Work;
